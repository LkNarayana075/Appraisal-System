export const environment = {
  production: true,

  // For Java URL
  serviceJavaUrl: 'http://localhost' + `:8080` + `/`,

  // For .NET URL

  servicedotnetUrl: 'http://localhost' + `:8080` + `/`,
  // For Python URL
  servicepythonUrl: 'http://localhost' + `:8080` + `/`,
  // For Example URL
  serviceexampleUrl: 'http://dummy.restapiexample.com' + `/api/v1` + `/`,
};
